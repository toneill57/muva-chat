# InnPilot Production Environment Variables (VPS)
# Copy this file to .env.local on your VPS server
# Location: /var/www/innpilot/.env.local

# =============================================================================
# Environment
# =============================================================================
NODE_ENV=production

# =============================================================================
# Application Configuration
# =============================================================================
# Production URL - IMPORTANT: Use your actual domain
NEXT_PUBLIC_APP_URL=https://innpilot.io

# Server port (PM2 will proxy this)
PORT=3000

# =============================================================================
# Supabase Configuration
# =============================================================================
# Get these from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# =============================================================================
# AI APIs - Conversation Memory System
# =============================================================================
# CRITICAL: Both keys required for conversation memory compression & search
# Claude Haiku: Conversation compression (~$0.001/compression)
# OpenAI Embeddings: text-embedding-3-large 1024d (~$0.0001/embedding)

# Anthropic API (Claude)
ANTHROPIC_API_KEY=your-anthropic-api-key
CLAUDE_MODEL=claude-3-5-haiku-20241022
CLAUDE_MAX_TOKENS=800

# OpenAI API (Embeddings)
OPENAI_API_KEY=your-openai-api-key

# =============================================================================
# JWT Authentication
# =============================================================================
# Generate a secure secret:
# openssl rand -base64 64
JWT_SECRET_KEY=your-production-jwt-secret-key

# =============================================================================
# Analytics (Plausible)
# =============================================================================
# Domain registered in Plausible Analytics
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=innpilot.io

# =============================================================================
# Production Optimizations
# =============================================================================
# Next.js production settings
NEXT_TELEMETRY_DISABLED=1

# =============================================================================
# Security Notes
# =============================================================================
# 1. NEVER commit this file with real credentials
# 2. Set file permissions: chmod 600 .env.local
# 3. Verify ownership: chown ubuntu:ubuntu .env.local
# 4. Restart PM2 after changes: pm2 restart innpilot
# 5. Test configuration: curl https://innpilot.io/api/health
